// Razorpay payout integration has been removed.
// Vendor payouts are now handled manually by the platform owner each week.
// These stub functions remain to satisfy existing import references.

export function hasRazorpayPayoutCredentials(): boolean {
  return false;
}
